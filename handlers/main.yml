---
- name: "restart php-fpm"
  sudo: yes
  service:
    name: "{{ php_fpm_service }}"
    state: restarted

- name: "restart hhvm"
  sudo: yes
  service:
    name: hhvm
    state: restarted

- name: "restart web server"
  sudo: yes
  service:
    name: "{{ php_web_server }}"
    state: restarted
